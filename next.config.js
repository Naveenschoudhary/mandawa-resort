/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["dummyimage.com", "assets.naveenschoudhary.com"],
  },
};

module.exports = nextConfig;
